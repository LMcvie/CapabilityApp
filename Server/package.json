{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server:dev": "nodemon server.js",
    "seeds": "mongo < ./db/teamSeeds.js && mongo < ./db/EngSeeds.js && mongo < ./db/ScrumSeeds.js && mongo < ./db/DesignSeeds.js && mongo < ./db/ProductSeeds.js && mongo < ./db/BusinessSeeds.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "mongodb": "^4.3.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
